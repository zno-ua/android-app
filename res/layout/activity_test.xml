<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >

    <ScrollView
        android:id="@+id/test_question_scroll_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="40dp"
        android:layout_marginBottom="10dp"
        android:orientation="vertical"
        android:scrollbarStyle="outsideInset" >

        <LinearLayout
            android:id="@+id/test_question_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:paddingEnd="5dp"
            android:paddingLeft="15dp"
            android:paddingRight="5dp"
            android:paddingStart="15dp" />
    </ScrollView>

    <Button
        android:id="@+id/test_skip_btn"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginEnd="15dp"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:layout_marginStart="15dp"
        android:layout_alignBottom="@id/test_question_scroll_container"
        android:text="@string/skip_question_text" />

</RelativeLayout>